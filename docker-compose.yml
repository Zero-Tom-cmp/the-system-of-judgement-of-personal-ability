services:
  backend:
    build: ./backend
    container_name: ability-backend
    ports:
      - "8000:8000"
    restart: always

  frontend:
    build: ./frontend
    container_name: ability-frontend
    ports:
      - "3000:3000"
    restart: always
    depends_on:
      - backend
